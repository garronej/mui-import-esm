import { useTheme } from "@mui/material/styles";
import Button from "@mui/material/Button";

console.log(useTheme);
console.log(Button); // This is an object with a default property instead of a function